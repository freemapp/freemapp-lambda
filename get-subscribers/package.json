{
  "name": "get-subscribers",
  "version": "1.0.0",
  "description": "api gateway proxy lambda to fetch dynamodb subscribers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.444.0"
  }
}
